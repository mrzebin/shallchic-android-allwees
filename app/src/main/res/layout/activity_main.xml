<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:orientation="vertical"
    android:paddingTop="10dp"
    >

    <TextView
        android:id="@+id/tv_appTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:gravity="center_horizontal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        >
    </TextView>

    <ListView
        android:id="@+id/lv_catalogue"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="@id/tv_appTitle"
        app:layout_constraintTop_toBottomOf="@id/tv_appTitle"
        android:divider="@color/colorPrimary"
        android:dividerHeight="0.5dp"
        android:cacheColorHint="@color/colorAccent"
        >
    </ListView>
<!--    android:stackFromBottom="true" 从底部开始显示-->


</androidx.constraintlayout.widget.ConstraintLayout>